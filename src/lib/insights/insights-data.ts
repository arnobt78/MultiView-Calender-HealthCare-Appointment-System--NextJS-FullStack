/**
 * Re-export — canonical implementation lives in @/lib/insights-data (API + SSR import path).
 */
export {
  getInsightsData,
  type GetInsightsDataInput,
  type InsightsDataOptions,
  type InsightsPayload,
} from "@/lib/insights-data";
