/**
 * Shared field labels for doctor portal schedule forms — `mb-1` + `text-xs` across cards.
 */

export const doctorSettingsFieldLabelClass =
  "mb-1 flex items-center gap-1.5 text-xs text-gray-700";

/** Mandatory asterisk sizing beside `text-xs` labels — color from `formRequiredMarkClass`. */
export const doctorSettingsRequiredMarkClass = "text-sm font-medium";
