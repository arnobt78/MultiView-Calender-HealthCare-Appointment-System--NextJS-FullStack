/**
 * Control-panel list-tab layout — re-exports shared section tokens for CP consumers.
 * Canonical definitions live in `section-page-layout.ts`.
 */

export {
  appSectionRootClass as controlPanelSectionRootClass,
  appSectionErrorBannerClass as controlPanelSectionErrorBannerClass,
} from "@/lib/section-page-layout";
