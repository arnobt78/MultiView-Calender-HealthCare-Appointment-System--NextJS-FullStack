/**
 * Defaults for doctor-owned appointment types (POST + idempotent seeds).
 * Globals keep their own seed values in `scripts/seed-test-user.ts`.
 */

export const DEFAULT_DOCTOR_OWNED_TYPE_BUFFER_MINUTES = 5;

export const DEFAULT_DOCTOR_OWNED_TYPE_SLOT_INTERVAL_MINUTES = 30;
